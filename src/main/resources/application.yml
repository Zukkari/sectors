spring:
  freemarker:
    cache: false
    suffix: '.ftl'
  data:
    mongodb:
      host: 'localhost'
      port: 27017
      database: 'admin'
      username: 'sectors'
      password: 'sectors'

sectors:
  insertion:
    run: false
    files: 'sectors.json'
